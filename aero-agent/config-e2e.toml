# Aero Agent Configuration - E2E Testing

[server]
# Backend WebSocket URL
backend_url = "ws://localhost:3000/ws"

# Unique node identifier from database
node_id = "cmkt6h7wd0002ephrk1e9dq1t"

# Node secret key from database
secret = "dev-secret-key-12345"

# Hostname of this server
hostname = "node1.example.com"

# Data directory for container volumes
data_dir = "/var/lib/aero"

# Maximum concurrent WebSocket connections
max_connections = 100

[containerd]
# Path to containerd socket
socket_path = "/run/containerd/containerd.sock"

# Containerd namespace for Aero containers
namespace = "aero"

[logging]
# Log level: trace, debug, info, warn, error
level = "debug"

# Log format: json or text
format = "json"
